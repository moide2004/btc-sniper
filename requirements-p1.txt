# Sous-ensemble P1 (socle données + mini-app) — pour hébergements à quota disque
# serré. scipy et arch ne servent qu'à partir de P2 (moteur statistique) : ils
# seront ajoutés à ce moment-là via requirements.txt.
#
# Installer avec :  pip install --no-cache-dir -r requirements-p1.txt
numpy==1.26.4
pandas==2.2.2
flask==3.0.3
requests==2.32.3
websocket-client==1.8.0
pyarrow==16.1.0
