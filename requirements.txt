# Moteur de probabilités BTC — dépendances épinglées (§2.6)
# Python 3.11+ requis.
numpy==1.26.4
pandas==2.2.2
scipy==1.13.1
flask==3.0.3
requests==2.32.3
websocket-client==1.8.0
arch==7.0.0
# VALIDÉ (§9, option A) : moteur parquet requis par les caches OHLCV (§7.2) —
# pandas ne lit/écrit pas le parquet sans engine ; pyarrow est la référence.
# Ajout au §2.6 approuvé par l'humain (format parquet conservé).
pyarrow==16.1.0
